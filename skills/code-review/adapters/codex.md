# Codex adapter

For application/code changes, evaluate `../policies/default.md` before reporting completion or committing/pushing.

If the policy requires review:
- load `../SKILL.md`
- use a fresh independent review context
- load an exact matching model profile under `../profiles/` only when the active model identity is known

The policy decides whether review is mandatory; the skill defines how review is performed.
