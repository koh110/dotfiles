interface:
  display_name: "Fail-Closed Automation"
  short_description: "破壊的・外部依存の自動化をfail-closedで設計・検証する"
  default_prompt: "Use $fail-closed-automation to design or review automation involving destructive operations, subprocesses, artifacts, or baselines so uncertainty fails closed."
